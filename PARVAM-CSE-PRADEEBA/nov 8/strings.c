#include<stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    char str[n];
    scanf("%s",str);
    printf("%s",str);

    //compile time
    // char str[]="hello";
    // char str[]={'a','b','c'};
}